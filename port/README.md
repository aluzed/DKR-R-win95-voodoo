# Future native port layer

Milestone 0 keeps its small standalone foundation under `src/`. As the decompiled game source is
integrated, hardware replacements, libultraship adapters and game-specific runtime code will move
under this directory so upstream matching code stays clearly separated.
